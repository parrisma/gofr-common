# =============================================================================
# GOFR Swarm Environment Configuration
# =============================================================================
# Copy this file to .env and customize values
# 
# Usage:
#   cp gofr-swarm.env .env
#   # Edit .env with your values
#   source .env
#   ./deploy-swarm.sh
# =============================================================================

# -----------------------------------------------------------------------------
# Security (REQUIRED - Change these!)
# -----------------------------------------------------------------------------
# JWT secret for GOFR service authentication
JWT_SECRET=change-this-to-a-secure-random-string

# Neo4j database password
NEO4J_PASSWORD=gofr-neo4j-password

# N8N credential encryption key
N8N_ENCRYPTION_KEY=change-this-encryption-key

# OpenAI API key for Open WebUI (optional)
OPENAI_API_KEY=

# Alternative OpenAI-compatible API base URL (optional)
# OPENAI_API_BASE_URL=https://api.openai.com/v1

# -----------------------------------------------------------------------------
# Backup Configuration
# -----------------------------------------------------------------------------
# Cron schedule for backups (default: 2 AM daily)
BACKUP_SCHEDULE=0 2 * * *

# Number of days to retain backups
BACKUP_RETENTION_DAYS=7

# -----------------------------------------------------------------------------
# Timezone
# -----------------------------------------------------------------------------
TZ=UTC

# -----------------------------------------------------------------------------
# Port Overrides (optional - defaults shown)
# -----------------------------------------------------------------------------
# Infrastructure
# NEO4J_HTTP_PORT=7474
# NEO4J_BOLT_PORT=7687
# CHROMA_PORT=8000

# GOFR Services (each has MCP, MCPO, Web ports)
# gofr-plot: 8050, 8051, 8052
# gofr-doc:  8040, 8041, 8042
# gofr-dig:  8030, 8031, 8032
# gofr-iq:   8020, 8021, 8022
# gofr-np:   8060, 8061, 8062

# Tools
# N8N_PORT=5678
# OPENWEBUI_PORT=3000

# -----------------------------------------------------------------------------
# Resource Limits (optional - defaults shown)
# -----------------------------------------------------------------------------
# NEO4J_MEMORY_LIMIT=2G
# CHROMA_MEMORY_LIMIT=2G
# GOFR_SERVICE_MEMORY_LIMIT=1G
# N8N_MEMORY_LIMIT=1G
# OPENWEBUI_MEMORY_LIMIT=2G
