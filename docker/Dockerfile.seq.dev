# SEQ Log Server — Development (ephemeral, no persistence)
# Based on official datalust/seq image.
# Data lives only in the container — destroyed on stop.
FROM datalust/seq:latest

ENV ACCEPT_EULA=Y
ENV SEQ_FIRSTRUN_ADMINPASSWORDHASH=""

# No volumes — everything is ephemeral
EXPOSE 5341 80
